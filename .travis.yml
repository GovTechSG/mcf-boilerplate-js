sudo: required
language: node_js
node_js:
  - "6"
  - "7"
  - "8"
services:
  - docker
stages:
  - verify
  - test
  - publish
install:
  - npm i -g npx
jobs:
  include:
    - stage: verify
      script:
        - lerna run build
    - stage: test
      script:
        - lerna run test
    - stage: publish
      script:
        - lerna run publish